type MessageApp = "WhatsApp" | "Telegram" | "Skype";

let application: MessageApp;

application = "WhatsApp"


type Fruit = {
    name: string;
    cor: string;
    quantidade ?: number;
};


let fruit:Fruit;

fruit = {
    name: "Mango",
    cor: "Pen",
    quantidade: 6,
}